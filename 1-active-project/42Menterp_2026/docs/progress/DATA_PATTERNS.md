# 데이터 처리 공통 패턴 가이드

> **작성일**: 2026-01-14
> **버전**: 1.0
> **적용 범위**: 모든 목록형 데이터 (전 에이전트 필수 준수)

---

## 🔴 필수 준수 사항

**모든 목록형 데이터**는 이 문서의 패턴을 **반드시** 따라야 합니다.

---

## 1. 적용 대상 메뉴

| # | 메뉴 | 경로 | 주요 데이터 |
|---|------|------|-------------|
| 1 | 고객 관리 | `/customers` | Customer |
| 2 | 매장 관리 | `/stores` | Store |
| 3 | 상품 관리 | `/products` | Product, Channel |
| 4 | 주문 관리 | `/orders` | Quotation, SalesOrder |
| 5 | 발주 관리 | `/purchase-orders` | PurchaseOrder |
| 6 | 정산 관리 | `/settlements` | Settlement, TaxInvoice |
| 7 | 계정 관리 | `/accounts` | User, Tenant |

---

## 2. 엑셀 업로드/다운로드 패턴

### 2.1 필수 기능 3종

```
┌─────────────────────────────────────────────────────────────────┐
│ [고객 관리]                                                     │
│                                                                 │
│ [양식 다운로드]  [엑셀 업로드]  [내보내기]       [+ 고객 등록]  │
│       ↓              ↓             ↓                            │
│   빈 양식 제공    일괄 등록     현재 데이터                     │
│   (필수/선택      (검증 포함)   다운로드                        │
│    필드 표시)                   (동일 양식)                     │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 양식 호환성 원칙

```
[양식 다운로드] ──→ [사용자 작성] ──→ [엑셀 업로드]
       │                                    │
       │         ┌─────────────┐            │
       └────────→│ 동일한 양식 │←───────────┘
                 └─────────────┘
                       │
                       ↓
              [내보내기] ──→ [재업로드 가능]
```

**핵심**: 다운로드 받은 데이터를 수정 후 **그대로 다시 업로드** 가능해야 함

### 2.3 엑셀 양식 구조 표준

```
┌────────────────────────────────────────────────────────────────┐
│ Row 1: 헤더 (한글 컬럼명)                                      │
│ Row 2: 필드 설명 (회색 배경, 필수/선택 표시)                   │
│ Row 3~: 데이터 영역                                            │
└────────────────────────────────────────────────────────────────┘
```

**예시 (고객 양식):**

| A | B | C | D | E | F |
|---|---|---|---|---|---|
| 고객명 | 사업자번호 | 대표자 | 담당자 | 연락처 | 이메일 |
| *필수* | 000-00-00000 | | | 010-0000-0000 | |
| ABC마케팅 | 123-45-67890 | 홍길동 | 김철수 | 010-1234-5678 | abc@example.com |

### 2.4 엑셀 파일 사양

| 항목 | 값 |
|------|-----|
| 파일 형식 | `.xlsx` (Excel 2007+) |
| 인코딩 | UTF-8 |
| 최대 행 수 | 10,000행 (권장), 50,000행 (최대) |
| 헤더 행 | Row 1 |
| 데이터 시작 | Row 2 또는 Row 3 (설명행 포함 시) |

---

## 3. 목록 일괄 처리 패턴

### 3.1 UI 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ 고객 관리                    [양식↓] [업로드↑] [+ 고객 등록]   │
├─────────────────────────────────────────────────────────────────┤
│ [검색...]              상태: [전체 ▼]     정렬: [최신순 ▼]     │
├─────────────────────────────────────────────────────────────────┤
│ ┌─ 5건 선택됨 ─────────────────────────────────────────────┐   │
│ │ [상태 변경 ▼]  [일괄 삭제]  [내보내기]      [선택 해제] │   │
│ └──────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│ ☑ │ 고객명      │ 사업자번호  │ 담당자 │ 상태   │ 액션       │
│───┼─────────────┼─────────────┼────────┼────────┼────────────│
│ ☑ │ ABC 마케팅  │ 123-45-6789 │ 홍길동 │ [활성] │ [⋮]       │
│ ☑ │ XYZ 컴퍼니  │ 987-65-4321 │ 김철수 │ [활성] │ [⋮]       │
│ ☐ │ 가나다 상사 │ 111-22-3333 │ 이영희 │ [정지] │ [⋮]       │
├─────────────────────────────────────────────────────────────────┤
│ ◀ 1 / 5 ▶                               총 95건 중 1-20 표시  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 선택 기능

| 기능 | 조작 | 설명 |
|------|------|------|
| 개별 선택 | 체크박스 클릭 | 단일 항목 선택/해제 |
| 전체 선택 | 헤더 체크박스 | 현재 페이지 전체 선택 |
| 범위 선택 | Shift + 클릭 | 시작~끝 범위 일괄 선택 |
| 전체 해제 | 툴바 [선택 해제] | 모든 선택 해제 |

### 3.3 일괄 액션 종류

| 액션 | 적용 메뉴 | 설명 |
|------|----------|------|
| 상태 변경 | 전체 | ACTIVE/PAUSED/TERMINATED 등 |
| 일괄 삭제 | 전체 | 제약조건 체크 후 삭제 |
| 내보내기 | 전체 | 선택된 항목만 엑셀 다운로드 |
| 일괄 발주 | 주문 | 선택된 수주 → 발주 전환 |
| 일괄 정산 | 발주 | 선택된 발주 → 정산 생성 |
| 세금계산서 발행 | 정산 | 선택된 정산 → 세금계산서 |

### 3.4 삭제 제약조건 (공통)

```typescript
// 삭제 불가 조건 체크 패턴
interface DeleteConstraint {
  canDelete: boolean;
  reason?: string;
  relatedCount?: number;
}

// 각 모델별 제약조건
const deleteConstraints = {
  Customer: ['연결된 매장 있음', '진행중인 주문 있음'],
  Store: ['진행중인 발주 있음', '미정산 건 있음'],
  Product: ['사용중인 주문 있음'],
  SalesOrder: ['발주 생성됨', '정산 생성됨'],
  PurchaseOrder: ['작업명세 확정됨', '정산 완료됨'],
  Settlement: ['세금계산서 발행됨', '입금 확인됨'],
};
```

---

## 4. API 패턴

### 4.1 필수 엔드포인트 (모든 리소스)

```typescript
// 기본 CRUD
GET    /api/{resource}              // 목록 조회 (페이지네이션, 검색, 필터)
POST   /api/{resource}              // 생성
GET    /api/{resource}/[id]         // 상세 조회
PUT    /api/{resource}/[id]         // 수정
DELETE /api/{resource}/[id]         // 삭제

// 🔴 일괄 처리 (필수)
PATCH  /api/{resource}/bulk         // 일괄 수정/삭제

// 🔴 엑셀 처리 (필수)
GET    /api/{resource}/template     // 엑셀 양식 다운로드
POST   /api/{resource}/import       // 엑셀 업로드 (일괄 등록)
GET    /api/{resource}/export       // 엑셀 내보내기
```

### 4.2 Bulk API 스펙

```typescript
// PATCH /api/{resource}/bulk
interface BulkRequest {
  ids: string[];
  action: 'update' | 'delete';
  data?: Record<string, unknown>;  // action이 'update'일 때
}

interface BulkResponse {
  success: boolean;
  summary: {
    total: number;
    processed: number;
    failed: number;
  };
  errors: Array<{
    id: string;
    reason: string;
  }>;
}
```

### 4.3 Import API 스펙

```typescript
// POST /api/{resource}/import
interface ImportRequest {
  data: Record<string, unknown>[];  // 파싱된 엑셀 데이터
  options: {
    skipDuplicates: boolean;    // 중복 건너뛰기
    updateExisting: boolean;    // 기존 데이터 업데이트
    dryRun: boolean;            // 검증만 (저장 안함)
  };
}

interface ImportResponse {
  success: boolean;
  summary: {
    total: number;
    created: number;
    updated: number;
    skipped: number;
    failed: number;
  };
  errors: Array<{
    row: number;
    field: string;
    value: unknown;
    message: string;
  }>;
}
```

### 4.4 Export API 스펙

```typescript
// GET /api/{resource}/export
// Query params: ids (선택), format (xlsx/csv), all (boolean)

// Response: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
// Content-Disposition: attachment; filename="{resource}_{date}.xlsx"
```

### 4.5 Template API 스펙

```typescript
// GET /api/{resource}/template
// Response: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
// Content-Disposition: attachment; filename="{resource}_template.xlsx"
```

---

## 5. 공용 컴포넌트 구조

### 5.1 디렉토리 구조

```
app/src/components/common/
├── data-table/
│   ├── index.tsx              # DataTable 메인 컴포넌트
│   ├── columns.tsx            # 컬럼 정의 헬퍼
│   ├── selection.tsx          # 선택 로직
│   ├── pagination.tsx         # 페이지네이션
│   └── filters.tsx            # 검색/필터
│
├── bulk-actions/
│   ├── index.tsx              # BulkActionToolbar
│   ├── delete-button.tsx      # 일괄 삭제 버튼
│   ├── status-change.tsx      # 상태 변경 드롭다운
│   └── export-button.tsx      # 내보내기 버튼
│
├── excel/
│   ├── import-dialog.tsx      # 엑셀 업로드 다이얼로그
│   ├── export-button.tsx      # 엑셀 내보내기 버튼
│   ├── template-button.tsx    # 양식 다운로드 버튼
│   └── utils.ts               # 엑셀 파싱/생성 유틸
│
└── hooks/
    ├── use-selection.ts       # 선택 상태 관리
    ├── use-bulk-action.ts     # 일괄 액션 처리
    └── use-excel.ts           # 엑셀 처리
```

### 5.2 사용 예시

```tsx
// customers/page.tsx
import { DataTable } from '@/components/common/data-table';
import { BulkActionToolbar } from '@/components/common/bulk-actions';
import { ExcelImportDialog, ExcelExportButton, TemplateButton } from '@/components/common/excel';
import { useSelection } from '@/components/common/hooks/use-selection';

export default function CustomersPage() {
  const { selectedIds, onSelect, onSelectAll, clearSelection } = useSelection();

  return (
    <div>
      {/* 헤더 */}
      <div className="flex justify-between">
        <h1>고객 관리</h1>
        <div className="flex gap-2">
          <TemplateButton resource="customers" />
          <ExcelImportDialog resource="customers" onSuccess={refetch} />
          <Button>+ 고객 등록</Button>
        </div>
      </div>

      {/* 일괄 액션 툴바 (선택 시 표시) */}
      {selectedIds.length > 0 && (
        <BulkActionToolbar
          selectedCount={selectedIds.length}
          onClear={clearSelection}
          actions={[
            { label: '상태 변경', type: 'status-change', options: ['ACTIVE', 'PAUSED', 'TERMINATED'] },
            { label: '일괄 삭제', type: 'delete' },
          ]}
        >
          <ExcelExportButton resource="customers" ids={selectedIds} />
        </BulkActionToolbar>
      )}

      {/* 데이터 테이블 */}
      <DataTable
        columns={columns}
        data={customers}
        selectable
        selectedIds={selectedIds}
        onSelect={onSelect}
        onSelectAll={onSelectAll}
      />
    </div>
  );
}
```

---

## 6. 필드별 양식 정의

### 6.1 Customer (고객)

| 컬럼명 | 필드 | 타입 | 필수 | 검증 | 세금계산서 |
|--------|------|------|------|------|-----------|
| 고객명 | name | string | ✅ | 1-100자 | ✅ |
| 사업자번호 | businessNo | string | | 000-00-00000 | ✅ |
| 대표자 | representative | string | | | ✅ |
| 담당자 | contactName | string | | | |
| 연락처 | contactPhone | string | | 전화번호 형식 | |
| 이메일 | contactEmail | string | | 이메일 형식 | ✅ |
| 주소 | address | string | | | ✅ |
| 계약시작일 | contractStart | date | | YYYY-MM-DD | |
| 계약종료일 | contractEnd | date | | YYYY-MM-DD | |
| 월예산 | monthlyBudget | number | | ≥0 | |
| 상태 | status | enum | | ACTIVE/PAUSED/TERMINATED | |
| 메모 | memo | string | | | |

### 6.2 Store (매장)

| 컬럼명 | 필드 | 타입 | 필수 | 검증 |
|--------|------|------|------|------|
| 매장명 | name | string | ✅ | 1-100자 |
| MID | mid | string | ✅ | 고유값 |
| 플레이스URL | placeUrl | string | | URL 형식 |
| 고객명 | customerName | string | | 기존 고객 매칭 |
| 사업자번호 | businessNo | string | | 000-00-00000 |
| 대표자 | representative | string | | |
| 담당자 | contactName | string | | |
| 연락처 | contactPhone | string | | |
| 이메일 | contactEmail | string | | |
| 주소 | address | string | | |
| 카테고리 | category | string | | |
| 상태 | status | enum | | ACTIVE/PAUSED/TERMINATED |
| 메모 | memo | string | | |

### 6.3 Product (상품)

| 컬럼명 | 필드 | 타입 | 필수 | 검증 |
|--------|------|------|------|------|
| 상품코드 | code | string | ✅ | 고유값 |
| 상품명 | name | string | ✅ | |
| 유형 | type | enum | ✅ | TRAFFIC/SAVE/REVIEW/DIRECTION |
| 설명 | description | string | | |
| 판매단가 | saleUnitPrice | number | ✅ | ≥0 |
| 매입단가 | costUnitPrice | number | ✅ | ≥0 |
| 채널코드 | channelCode | string | | 기존 채널 매칭 |
| 활성여부 | isActive | boolean | | true/false |

### 6.4 SalesOrder (수주)

| 컬럼명 | 필드 | 타입 | 필수 | 검증 |
|--------|------|------|------|------|
| 수주번호 | salesOrderNo | string | | 자동생성 |
| 고객명 | customerName | string | ✅ | 기존 고객 매칭 |
| 매장MID | storeMid | string | ✅ | 기존 매장 매칭 |
| 상품코드 | productCode | string | ✅ | 기존 상품 매칭 |
| 키워드 | keyword | string | | |
| 일일수량 | dailyQty | number | ✅ | ≥1 |
| 시작일 | startDate | date | ✅ | YYYY-MM-DD |
| 종료일 | endDate | date | ✅ | YYYY-MM-DD |
| 단가 | unitPrice | number | | 자동 (상품 기준) |
| 메모 | memo | string | | |

---

## 7. 구현 체크리스트

### 새 목록 페이지 구현 시

```markdown
## 필수 체크리스트

### UI
- [ ] 체크박스 선택 컬럼 추가
- [ ] 헤더 전체선택 체크박스
- [ ] Shift+클릭 범위 선택
- [ ] 선택 시 플로팅 툴바 표시
- [ ] 양식 다운로드 버튼
- [ ] 엑셀 업로드 버튼
- [ ] 내보내기 버튼

### API
- [ ] GET /api/{resource} (목록)
- [ ] POST /api/{resource} (생성)
- [ ] GET /api/{resource}/[id] (상세)
- [ ] PUT /api/{resource}/[id] (수정)
- [ ] DELETE /api/{resource}/[id] (삭제)
- [ ] PATCH /api/{resource}/bulk (일괄)
- [ ] GET /api/{resource}/template (양식)
- [ ] POST /api/{resource}/import (업로드)
- [ ] GET /api/{resource}/export (내보내기)

### 엑셀
- [ ] 양식 필드 정의 (필수/선택)
- [ ] 업로드 검증 규칙 구현
- [ ] 중복 처리 로직
- [ ] 오류 메시지 한글화
- [ ] 양식/내보내기 호환성 확인
```

---

## 8. 권장 라이브러리

| 라이브러리 | 용도 | 설치 |
|-----------|------|------|
| xlsx | 엑셀 파싱/생성 | `npm i xlsx` |
| react-spreadsheet-import | 임포트 UI | `npm i react-spreadsheet-import` |
| @tanstack/react-table | 테이블 | 이미 설치됨 (shadcn) |
| file-saver | 파일 다운로드 | `npm i file-saver` |

---

**이 문서는 프로젝트 전체 필수 지침입니다. 모든 에이전트는 이 패턴을 준수해야 합니다.**
