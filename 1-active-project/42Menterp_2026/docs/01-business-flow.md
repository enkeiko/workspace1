# 42Ment ERP 2026 - 비즈니스 플로우

## 문서 정보

- **작성일**: 2025-12-31
- **버전**: 1.0
- **관련 문서**: 00-executive-summary.md, 02-data-model.md

---

## 1. 광고대행업 핵심 이해

### 1.1 광고대행업 비즈니스 모델

```
┌─────────────────────────────────────────────────────────────┐
│                    광고대행업 수익 구조                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   [고객]                        [광고대행사]                 │
│     │                              │                        │
│     │ 청구: 100만원                │                        │
│     │ ────────────────────────────>│                        │
│     │                              │                        │
│     │                              │ 지급: 70만원            │
│     │                              │ ────────────────────>   │
│     │                              │                [거래처] │
│     │                              │                        │
│   수익 = 청구(100) - 지급(70) = 30만원 (이익률 30%)         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 핵심 관리 포인트

| 관리 항목 | 질문 | ERP 기능 |
|----------|------|----------|
| **매출** | 이번 달 얼마 청구했나? | Order, Invoice |
| **비용** | 이번 달 얼마 나갔나? | PurchaseOrder |
| **이익** | 얼마 남았나? | ProfitAnalysis |
| **미수금** | 받을 돈이 얼마인가? | Invoice (unpaid) |
| **미지급금** | 줄 돈이 얼마인가? | PurchaseOrder (unpaid) |

---

## 2. 전체 업무 플로우

### 2.1 메인 플로우 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                    42Ment ERP 2026 핵심 플로우                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [1. 상담] ─────────────────────────────────────────────────    │
│    │ Consultation 생성                                          │
│    │ - 채널: 카카오톡, 전화, 이메일, 대면                        │
│    │ - 내용: 고객 요구사항 기록                                  │
│    │ - 액션아이템: 다음 단계 정의                                │
│    ▼                                                            │
│  [2. 견적] ─────────────────────────────────────────────────    │
│    │ Quotation 생성                                             │
│    │ - 상품 선택 (Product)                                      │
│    │ - 단가 적용 (기본가 or 고객별 특별가)                       │
│    │ - 광고상품: 기간/일일건수 설정                              │
│    │ - PDF 견적서 생성 및 발송                                  │
│    ▼                                                            │
│  [3. 계약확정] ─────────────────────────────────────────────    │
│    │ Quotation → Order 전환                                     │
│    │ - 상태: draft → confirmed                                  │
│    │ - 계약서 첨부 (선택)                                       │
│    ▼                                                            │
│  [4. 구매발주] ← ⭐ 현재 누락된 핵심 기능 ⭐                     │
│    │ Order → PurchaseOrder 생성                                 │
│    │ - 거래처 선택 (Supplier)                                   │
│    │ - 매입단가 적용 (SupplierProduct)                          │
│    │ - 작업 상세 정보 입력                                      │
│    ▼                                                            │
│  [5. 작업실행] ─────────────────────────────────────────────    │
│    │ PurchaseOrder 상태: in_progress                            │
│    │ - 거래처에 발주 전달                                       │
│    │ - WorkHistory 기록 (매장별 작업 내역)                      │
│    │ - Task로 진행상황 추적                                     │
│    ▼                                                            │
│  [6. 검수/완료] ────────────────────────────────────────────    │
│    │ PurchaseOrder 상태: completed                              │
│    │ Order 상태: completed                                      │
│    │ - 작업 결과물 확인                                         │
│    │ - 고객에게 보고 (선택)                                     │
│    ▼                                                            │
│  [7. 청구] ─────────────────────────────────────────────────    │
│    │ Invoice 생성 (세금계산서)                                  │
│    │ - PDF 세금계산서 생성                                      │
│    │ - 고객에게 발송                                            │
│    ▼                                                            │
│  [8. 입금] ─────────────────────────────────────────────────    │
│    │ Payment 기록                                               │
│    │ - Invoice와 매칭 (자동/수동)                               │
│    │ - Order.paidAmount 업데이트                                │
│    ▼                                                            │
│  [9. 손익확정] ─────────────────────────────────────────────    │
│    │ ProfitAnalysis 자동 계산                                   │
│    │ - 매출: Order.totalAmount                                  │
│    │ - 비용: Σ PurchaseOrder.totalAmount                       │
│    │ - 이익: 매출 - 비용                                        │
│    │ - 이익률: 이익 / 매출 × 100                               │
│    ▼                                                            │
│  [10. 대시보드] ────────────────────────────────────────────    │
│    • 기간별 매출/비용/이익                                       │
│    • 고객별/매장별 손익                                          │
│    • 미수금 현황                                                 │
│    • 거래처별 지출                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 각 단계별 상세

### 3.1 상담 (Consultation)

**목적**: 고객 요구사항 파악 및 기록

**주요 필드**:
- `customerId`: 고객 연결
- `storeId`: 매장 연결 (선택)
- `consultationChannel`: 상담 채널 (kakao, phone, email, meeting)
- `consultationDate`: 상담 일시
- `consultationContent`: 상담 내용
- `actionItems`: 액션 아이템

**상태 흐름**: 없음 (단순 기록)

**연결 관계**:
- → Quotation 생성으로 이어짐
- `relatedQuotationId`로 연결

---

### 3.2 견적 (Quotation)

**목적**: 고객에게 제안할 서비스와 금액 정의

**주요 필드**:
- `quotationNumber`: 견적번호 (자동 생성)
- `customerId`, `storeId`: 고객/매장 연결
- `quotationDate`: 견적 작성일
- `validUntil`: 유효기간
- `totalAmount`: 합계 금액
- `status`: draft → sent → accepted → rejected

**QuotationItem 필드** (광고상품 특성):
- `productId`, `productName`: 상품
- `quantity`, `unitPrice`, `totalPrice`: 수량/단가/합계
- `startDate`, `endDate`: 광고 시작/종료일
- `dailyCount`: 일일 건수/유입타수
- `totalDays`: 총 진행일수 (자동 계산)

**단가 적용 우선순위**:
1. CustomerProductPrice (고객별 특별가) - 있으면 우선 적용
2. Product.unitPrice (기본 판매가) - 없으면 기본가

**상태 흐름**:
```
draft → sent → accepted → Order 생성
                └→ rejected (종료)
```

---

### 3.3 주문 확정 (Order)

**목적**: 확정된 계약, 실제 매출 기록

**주요 필드**:
- `orderNumber`: 주문번호 (자동 생성)
- `quotationId`: 견적서 연결
- `customerId`, `storeId`: 고객/매장 연결
- `orderDate`: 주문일
- `totalAmount`: 총 금액 (매출)
- `paidAmount`: 입금액
- `unpaidAmount`: 미수금 (자동 계산)
- `status`: pending → confirmed → in_progress → completed

**Quotation → Order 전환 시**:
- QuotationItem → OrderItem 복사
- 광고상품 특성(startDate, endDate, dailyCount) 유지
- Quotation.status = accepted

**상태 흐름**:
```
pending → confirmed → in_progress → completed
                               └→ 구매발주 생성
```

---

### 3.4 구매발주 (PurchaseOrder) ⭐ 핵심 신규 기능

**목적**: 거래처에 발주, 비용 추적

**주요 필드**:
- `poNumber`: 발주번호 (자동 생성)
- `orderId`: 연결된 판매주문
- `supplierId`: 거래처
- `storeId`: 작업 대상 매장
- `poDate`: 발주일
- `deliveryDate`: 납기일
- `totalAmount`: 발주 금액 (비용)
- `status`: draft → sent → in_progress → completed

**PurchaseOrderItem 필드**:
- `productId`, `productName`: 상품
- `quantity`, `unitPrice`, `totalPrice`: 수량/매입단가/합계
- `workDetails`: 작업 상세 (키워드, URL 등) - JSON

**매입단가 적용**:
- SupplierProduct에서 거래처별 매입단가 조회
- 없으면 수동 입력

**Order와의 관계**:
- 1개 Order → N개 PurchaseOrder 가능
- (예: 블로그 업체, 리뷰 업체에 각각 발주)

**상태 흐름**:
```
draft → sent → in_progress → completed
```

---

### 3.5 작업 실행 및 검수

**작업 추적 방법**:

1. **Task 활용**:
   - `orderId`로 주문 연결
   - `taskType`: blog, review, seo, design 등
   - `status`: pending → in_progress → completed
   - `TimeEntry`로 작업 시간 기록

2. **WorkHistory 활용** (매장별):
   - `storeId`로 매장 연결
   - `orderId`로 주문 연결
   - `workType`: 작업 유형
   - `workDetail`: 작업 내용

---

### 3.6 청구 (Invoice)

**목적**: 세금계산서 발행, 미수금 관리

**주요 필드**:
- `invoiceNumber`: 세금계산서 번호
- `orderId`, `customerId`: 주문/고객 연결
- `invoiceDate`: 발행일
- `dueDate`: 납기일
- `amount`: 청구 금액
- `isPaid`: 입금 여부
- `paidDate`: 입금일
- `status`: pending → sent → paid

**상태 흐름**:
```
pending → sent → paid (입금 확인)
           └→ overdue (연체, 선택적)
```

---

### 3.7 입금 (Payment) ⭐ 신규 기능

**목적**: 입금 기록, 계산서 매칭

**주요 필드**:
- `invoiceId`: 연결된 계산서 (선택)
- `customerId`: 고객
- `paymentDate`: 입금일
- `amount`: 입금액
- `depositorName`: 입금자명
- `matchStatus`: auto_matched / manual_matched / unmatched

**매칭 로직**:
```
1. 자동 매칭 조건:
   - 입금액 == 계산서 금액
   - 입금자명 ≈ 고객명 or 사업자번호 포함

2. 수동 매칭:
   - 미매칭 입금 목록 표시
   - 관리자가 수동으로 Invoice 선택
```

**입금 처리 시**:
- Invoice.isPaid = true
- Invoice.paidDate = paymentDate
- Order.paidAmount += amount
- Order.unpaidAmount = totalAmount - paidAmount

---

### 3.8 손익분석 (ProfitAnalysis) ⭐ 핵심 신규 기능

**목적**: 주문별 수익성 자동 계산

**계산 공식**:
```
매출 (revenue) = Order.totalAmount
비용 (costOfGoods) = Σ PurchaseOrder.totalAmount (해당 Order 연결된 것)
이익 (grossProfit) = 매출 - 비용
이익률 (profitMargin) = (이익 / 매출) × 100
```

**계산 시점**:
- Order.status = completed 될 때 자동 계산
- 또는 수동 트리거

**집계 레벨**:
- 주문별 손익
- 고객별 손익 (고객의 모든 주문 합계)
- 매장별 손익 (매장의 모든 주문 합계)
- 기간별 손익 (일/주/월/분기/연)

---

## 4. 대시보드 설계

### 4.1 핵심 지표 카드

```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   이번 달    │ │   이번 달    │ │   이번 달    │ │    미수금    │
│    매출      │ │    비용      │ │    이익      │ │    현황      │
│  ₩5,000만   │ │  ₩3,500만   │ │  ₩1,500만   │ │   ₩800만    │
│   (30%)     │ │             │ │   (30%)     │ │   (5건)     │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

### 4.2 추이 그래프

- 월별 매출/비용/이익 추이 (최근 12개월)
- 고객별 매출 Top 10
- 거래처별 지출 Top 10

### 4.3 미수금 관리

| 고객명 | 주문번호 | 금액 | 청구일 | 경과일 | 상태 |
|--------|---------|------|--------|-------|------|
| A사 | ORD-2025-001 | ₩100만 | 12/15 | 16일 | 🔴 연체 |
| B사 | ORD-2025-002 | ₩50만 | 12/20 | 11일 | 🟡 임박 |
| C사 | ORD-2025-003 | ₩200만 | 12/28 | 3일 | 🟢 정상 |

---

## 5. 데이터 흐름 요약

```
                    ┌─────────────┐
                    │  Customer   │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌─────────┐  ┌─────────┐  ┌─────────┐
        │  Store  │  │Quotation│  │Consulta │
        └────┬────┘  └────┬────┘  │  tion   │
             │            │       └─────────┘
             │            ▼
             │      ┌─────────┐
             └─────>│  Order  │<─── 매출
                    └────┬────┘
                         │
           ┌─────────────┼─────────────┐
           ▼             ▼             ▼
     ┌──────────┐  ┌──────────┐  ┌──────────┐
     │ OrderItem│  │ Invoice  │  │ Purchase │<─── 비용
     └──────────┘  └────┬─────┘  │  Order   │
                        │        └────┬─────┘
                        ▼             ▼
                   ┌─────────┐  ┌──────────┐
                   │ Payment │  │ Supplier │
                   └─────────┘  └──────────┘
                        │
                        ▼
                   ┌─────────────┐
                   │ProfitAnalysis│<─── 이익 = 매출 - 비용
                   └─────────────┘
```

---

## 6. 다음 단계

- **02-data-model.md** - 위 플로우를 지원하는 데이터 모델 상세
- **각론 문서** - 모듈별 상세 설계

---

## 변경 이력

| 버전 | 날짜 | 변경 내역 | 작성자 |
|------|------|----------|--------|
| 1.0 | 2025-12-31 | 초안 작성 | AI Assistant |



